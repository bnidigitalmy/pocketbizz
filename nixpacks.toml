[phases.setup]
nixPkgs = ['nodejs_20', 'npm-10_x', 'openssl']

[nix]
channel = "nixos-24.05"

[phases.install]
cmds = ['npm ci --legacy-peer-deps']

[phases.build]
cmds = ['npm run build', 'npm run db:push']

[start]
cmd = 'npm start'
