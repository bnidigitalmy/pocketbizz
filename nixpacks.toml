[phases.setup]
nixPkgs = ['nodejs_20', 'npm-10_x', 'openssl']

[nix]
channel = "nixos-24.05"

[phases.install]
cmds = ['npm ci --legacy-peer-deps || npm install --legacy-peer-deps']

[phases.build]
cmds = ['rm -rf dist', 'npm run build', 'npm run db:push']

[start]
cmd = 'npm start'
